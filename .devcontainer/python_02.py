
# Creating two lists
list1 = [1, 2, 3, 4, 5]
list2 = ['apple', 'banana', 'cherry', 'date', 'elderberry']

# Using the extend() method to join list2 to list1
list1.extend(list2)

# Alternatively, you can use the + operator to concatenate the lists
# joined_list = list1 + list2

print(list1)
